extend-exclude = ["BinjoAnalyzer", "__pycache__"]
target-version = "py311"

[lint]
select = ["E", "F"]
ignore = [
    "E501", # data tables / hex dumps in this codebase run long; not worth reflowing
]
